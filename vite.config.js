const config = {
	resolve: {
		alias: {
			buffer: 'buffer',
			crypto: 'crypto-browserify',
			events: 'events/',
			path: 'path-browserify',
			stream: 'stream-browserify'
		}
	}
};

export default config;
